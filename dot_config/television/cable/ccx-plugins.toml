[metadata]
name = "ccx-plugins"
description = "Browse available Claude Code plugins"
requirements = ["ccx"]

[source]
command = "ccx plugins list"
display = "{}"
output = "{split: :1}"

[ui]
layout = "landscape"

[ui.preview_panel]
size = 50
header = "Plugin: {split: :1}"
